name: Test Application

supportedOperatingSystems:
  - LINUX
  - WINDOWS

startCommand:
  launcherPath: "{{M:SELF}}/bin/gradle-plugin-test-project{{WINDOWS:.bat}}"

  parameters:
    - template: test-params

#runtimeDependencies:
#  - external/software:v1.0.0
