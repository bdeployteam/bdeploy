# Properties that are passed to the JVM started by the native launcher application
# Lines starting with a comment are ignored

# Enable remote debugging (waits for debugger before starting)
# -agentlib:jdwp=transport=dt_socket,server=y,address=8000,suspend=y

# Increment memory if required
-Xmx256m